# Testing and Artifact Contract

- Provide verification commands and expected outcome criteria.
- For each key result, include:
  - evidence paths
  - verification command(s)
  - status (`proposed`/`verified`/`uncertain`)
- Prefer storing machine-readable summaries (`json`/`yaml`) alongside narrative markdown.
- If visualization is generated, include the source data path and plotting script/command.
